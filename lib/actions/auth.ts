'use server'

// Magic-link redemption lives in app/api/auth/validate-magic-link/route.ts.
// The previous server action used a deprecated tempPassword pattern and was
// not called from anywhere.

export {}
